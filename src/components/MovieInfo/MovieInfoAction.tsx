import { Button } from "@chakra-ui/react";
import React from "react";

export const MovieInfoAction = () => {
  return (
    <Button bg="blue.50" _hover={{ bg: "blue.100" }} mt={3}>
      Book Tickets
    </Button>
  );
};
